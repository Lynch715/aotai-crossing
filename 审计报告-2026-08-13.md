# 《穿越鳌太线》全面审计报告

日期：2026-08-13。范围：src/ 全部 28 个模块、build.mjs、测试、dist 产物、docs/source 原始素材与设计文档。
测试与构建现状：`npm test` 455/455 通过，`npm run build` 正常产出。数据保真度良好——24 节点、12 人物、装备与四季表与 docx 素材逐条核对一致。

问题按严重程度分三档。**架构是好的（前端权威 + LLM 只提议），但相当多设计好的机制根本没接上线**——代码存在、测试通过、运行时永远走不到。这类"死机制"是"不尽如人意"的主要来源。

---

## 一、严重：机制实际失效

### 1. 掷骰从第 5 回合起变成定数
`src/ui/app.js:782`：`种子 = state.meta.随机种子 + 最近回合.length`，而 `最近回合` 被封顶在 4 条（`>4 就 shift`）。于是第 5 回合起每回合种子恒定、每回合只掷一次骰 → **掷出的永远是同一个数**。所有"勉强 70%"选项从此要么永远成功、要么永远失败。刷新页面后 `最近回合` 清空，种子序列又从头重放。
修法：用状态里的回合计数（如 `day*3 + slot序号`，或在 state 里加 `回合数`）做种子偏移，别用一个会封顶的数组长度。`turn.js:58` 缺省 rng 的 `makeRng(state.meta.随机种子)` 同病（每回合重建、恒定首值），只是被 app 传参掩盖。

### 2. 发给模型的"最近回合"全是 `[object Object]`
`app.js:817` 往 `最近回合` 推的是对象 `{标题, 剧情摘要, 选项, 判定}`；`prompt.js:122` 直接 `近.join('\n\n---\n\n')`。运行时模型收到的上文是三行 `[object Object]`。**这是剧情重复、前后不衔接的直接原因之一**。测试没抓到是因为 prompt.test 全部传字符串，没有集成测试覆盖 app→prompt 这条线。

### 3. 选项 cost 从未结算
`clampCost` 夹完的净代价在 `turn.js:52` 算出来后就再没人用。选项申报的 体力/金钱/时段 代价全是摆设——"明码标价"只有 require 半边真。连带后果：**金钱出发后永远不变**（唯一变动是终点罚款），素材里"可想办法筹钱"也无从谈起。

### 4. 伤病系统不存在
`state.pc.伤病` 初始化为空数组后全项目零写入：`validateProposal` 没有伤病字段，引擎也没有事件源。于是"重伤拖 2 天致死"的结局（ending.js:15）是死代码；医药包、布洛芬、补液盐、护膝、帐篷杆修理包等一批装备**买了没有任何机制效果**。

### 5. "被救援"结局不可达
`flags.已求救` 全项目无写入方。没有求救按钮、没有 LLM 提议通道。GPS/卫星信标、卫星电话——采购页郑重警告"不带就无法求救"的东西——**带了也无法求救**。

### 6. 进食 / 休整机制零调用
`consume.js` 的 `eatHot / eatCold / rest` 写了、测了，但没有任何调用方。炉头、气罐、备用气罐、冻干餐、额外冻干餐、路餐、折凳全部无效；体力唯一回复渠道是过夜。兜底选项"原地休整，恢复体力"实际效果是**扣**一次步进体力。

### 7. 食物数量语义断裂
商店把一切物品按 `数量:1` 入包（screen-shop.js:128），而 `dailyUpkeep` 每天扣 2 份主粮。结果：主粮（素材注明"按 7 天计"）第一晚就被扣光，第二天起 `断粮:true`——而且**断粮的返回值没人接**（turn.js:70 调用后丢弃），断粮没有任何后果。水（3L 水袋"可 0-4L 滑动"）、"水仅在有水源节点可补"整套也未实现。

### 8. 队伍永远只有开局 2 人
素材要求"其他人在徒步过程中慢慢接触"，npcs.js 注释说踏雪、猛蛇过江"途中遭遇"。但 `validateProposal` 只有离队、没有入队通道；模型想让新角色说话还会被"不在队"驳回。**12 个精心录入的人物，一局实际只能用到 2 个，且只减不增。**

---

## 二、中等：流程与一致性

### 9. 结局页是死胡同
`renderEnding` 没有任何按钮。自动存档 phase=结局 → 刷新永远回结局页。**重开一局的唯一办法是手动清 localStorage。** 整个应用也没有任何返回/菜单导航；save.js 定义的 slot1-3、`deleteSave`、`listSaves` 全无 UI 调用——手动存档功能整体缺席。

### 10. 有存档时永远跳过配置屏
启动逻辑（app.js:1139）见档就直进 game。换浏览器/清了 key/dist 拷给朋友时，第一回合报"还没填 API key"，且无路可回配置屏。

### 11. NPC 初始状态被丢弃
`createInitialState` 给全员 `状态:'正常'`。王大鹏"膝盖旧伤复发"、周涛"脚踝扭伤"只活在抽卡卡片与 system prompt 里；user message 的在队栏却报"正常"，两处自相矛盾，模型无所适从。`setNpcStatus` 除离队外无人调用，状态终局不变。

### 12. 判定失败没有机制后果
失败只是让模型把故事写成失败；数值上与成功完全一样（步进消耗照扣、无额外代价）。spec 里"回馈不同数值"未落地。结合问题 3，成功/失败在引擎层是等价的。

### 13. 推进全凭模型心情
玩家无法主动选择去向；模型不写 `去向建议` 就原地踏步。没有天数上限、断粮无后果（问题 7）、体力每晚回 25——**游戏可以无限不推进也不恶化**，缺一个把人往前赶的压力源。

### 14. 请求无超时、无取消
client.js 的 `timeout` 错误分类永远走不到——没人创建带定时的 AbortController，UI 也没有取消按钮。API 一旦挂起就是永久转圈，选项还处于禁用态。

### 15. 其他中等项
捏人"经验微调 ±10"有字段有算法、无 UI 控件。天气只靠模型随口一句"天气建议"驱动，不给就永远"晴 1 级"；素材要求的"季节固定事件""主线预警倒计时"未实现，`flags.触发过的事件id` 是孤儿字段。temperature / maxTokens 无 UI（截断警告让玩家"调大最大输出"，但没地方调）。

---

## 三、轻微

- README 写"433 个测试"，实际 455（陈旧）。
- `parser.js` 万象段落只认"鳌太万象"字样，模型写"万象"就整段丢失。
- 流式阶段打字机直出原文，玩家会先看到 `[剧情标题]`、`<<<STATE>>>` 等协议头，解析完才替换（代码已知，属体验毛刺）。
- `build.test.js` 往 `src/engine/` 写临时探针文件再删；删除一旦失败（本次审计的沙盒里就发生了）会连锁挂掉 5 个用例并污染仓库。临时文件应写到 `os.tmpdir()`。
- `screen-game` 的 `APP渲染舞台` 里有一行死代码：`const { gameViewModel: gvm } = { gameViewModel }`。
- 判定成功也不给任何奖励反馈（经验不成长——`户外经验` 全程静态）。

---

## 四、修复优先级建议

第一批（直接决定"好不好玩"）：#2 最近回合序列化 → #1 掷骰种子 → #9 结局页重开 + 导航 → #3 cost 结算。
第二批（把死机制接上线）：#6 进食/休整做成原生按钮或选项类型 → #7 食物按份数购买与消耗 → #5 求救（原生按钮，有设备才亮）→ #4 伤病（LLM 提议 + 校验通道）。
第三批（内容纵深）：#8 途中入队（踏雪/猛蛇过江遭遇事件）→ #13 推进压力（断粮扣体力、天数上限或补给倒计时）→ #11 NPC 初始状态入 state。

一个总的观察：这个项目的单元测试很扎实（455 个），但**全部测的是模块内部**，一条端到端的集成测试都没有——#1、#2、#3、#6 这类"两个模块之间的线没接上"的问题恰好全部漏网。建议补一条"假模型跑完 10 回合"的集成测试，上面一半的问题会当场现形。
